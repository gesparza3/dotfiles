#!/usr/bin/env bash
set -euo pipefail

plugdir="$HOME/.config/ranger/plugins"
mkdir -p "$plugdir"

if [ ! -d "$plugdir/ranger_devicons" ]; then
  git clone --depth=1 https://github.com/alexanderjeurissen/ranger_devicons "$plugdir/ranger_devicons"
fi
